# PyInstaller VSVersionInfo resource for the Windows bundle.
# Drives the name Windows shows in the firewall prompt, Task Manager,
# the exe properties dialog, and similar OS surfaces.
#
# Template: the @VERSION_TUPLE@ / @VERSION@ placeholders are filled from
# agent/VERSION by swarpius-windows.spec at build time. Do not hardcode a
# version here — bump agent/VERSION instead.
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(@VERSION_TUPLE@),
    prodvers=(@VERSION_TUPLE@),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          '040904B0',
          [
            StringStruct('CompanyName', 'Paraseva Ltd'),
            StringStruct('FileDescription', 'Swarpius'),
            StringStruct('FileVersion', '@VERSION@'),
            StringStruct('InternalName', 'Swarpius'),
            StringStruct('OriginalFilename', 'swarpius.exe'),
            StringStruct('ProductName', 'Swarpius'),
            StringStruct('ProductVersion', '@VERSION@'),
            StringStruct('LegalCopyright', 'Copyright © 2026 Paraseva Ltd'),
          ],
        ),
      ],
    ),
    VarFileInfo([VarStruct('Translation', [0x0409, 0x04B0])]),
  ],
)
