# Voice name matching a <name>.wav in voices/. Leave empty for the default
# voice. No quotes — this file is read by docker-compose's env_file parser,
# which in some versions keeps surrounding quotes as literal characters
# (TTS_VOICE="lois" would look for "lois".wav, not lois.wav).
TTS_VOICE=
